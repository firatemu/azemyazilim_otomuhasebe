'use client';

/**
 * MUI Server-Safe Primitives
 * 
 * These components are exported as 'use client' boundaries so they can be
 * imported and used safely within Server Components without marking the 
 * entire Server Component as a client component.
 */

export {
    Box,
    Typography,
    Card,
    CardContent,
    Grid,
    Button,
    IconButton,
    Divider,
    Skeleton,
    Stack,
    Container,
    Paper,
    Avatar,
    Chip,
    Tooltip,
    CircularProgress,
    Alert,
    Toolbar
} from '@mui/material';
