
describe('Health Check', () => {
    it('should be true', () => {
        expect(true).toBe(true);
    });

    it('should calculate basic arithmetic', () => {
        const sum = 1 + 1;
        expect(sum).toBe(2);
    });
});
